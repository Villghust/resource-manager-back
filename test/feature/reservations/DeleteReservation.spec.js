import chai from 'chai';

import req from '../../config/request.js';

const { expect } = chai;
const request = req();

const ROUTE = '/users/cost';

describe('DeleteReservation', () => {
    describe('DELETE /reservation/:reservation_id', () => {
        context.skip('', () => {
            it('', async () => {});
        });
    });
});
