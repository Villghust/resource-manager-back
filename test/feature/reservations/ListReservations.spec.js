import chai from 'chai';

import req from '../../config/request.js';

const { expect } = chai;
const request = req();

const ROUTE = '/users/cost';

describe('ListReservations', () => {
    describe('GET /reservation', () => {
        context.skip('', () => {
            it('', async () => {});
        });
    });
});
