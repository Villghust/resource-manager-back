import chai from 'chai';

import req from '../../config/request.js';

const { expect } = chai;
const request = req();

const ROUTE = '/users/cost';

describe('TotalUsersCost', () => {
    describe('GET /users/cost', () => {
        context.skip('', () => {
            it('', async () => {});
        });
    });
});
