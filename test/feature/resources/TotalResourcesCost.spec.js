import chai from 'chai';

import req from '../../config/request.js';

import ResourceType from '../../../src/app/enums/ResourceTypeEnum.js';

const { expect } = chai;
const request = req();

const COST_ROUTE = '/cost';
const ROUTE = '/resources/available';

describe('TotalResourcesCost', () => {
    describe('GET /resources/cost', () => {
        context.skip('', () => {
            it('', () => {});
        });
    });
});
